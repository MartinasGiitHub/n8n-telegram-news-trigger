name: Telegram News Trigger

on:
  schedule:
    - cron: '0 8 * * *' # täglich um 8:00 UTC = 10/11 Uhr 🇩🇪
  workflow_dispatch:

jobs:
  trigger-n8n:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Telegram News Workflow
        run: curl -X POST https://primary-production-7e351.up.railway.app/webhook-test/telegram-news
